{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ReviewOutput",
  "type": "object",
  "additionalProperties": false,
  "required": ["summary", "findings"],
  "properties": {
    "summary": {"type": "string", "maxLength": 10000},
    "findings": {
      "type": "array", "maxItems": 100,
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["path", "line", "category", "severity", "confidence", "title", "body"],
        "properties": {
          "path": {"type": "string", "minLength": 1, "maxLength": 1024},
          "line": {"type": "integer", "minimum": 1},
          "category": {
            "enum": [
              "BUG", "CORRECTNESS", "BEHAVIOR", "REGRESSION", "SECURITY",
              "DATA_INTEGRITY", "TRANSACTION", "CONCURRENCY", "NULL_SAFETY",
              "ERROR_HANDLING", "API_CONTRACT", "PERFORMANCE", "RESOURCE_LEAK",
              "TESTING", "SIMPLIFICATION"
            ]
          },
          "severity": {"enum": ["CRITICAL", "HIGH", "MEDIUM", "LOW"]},
          "confidence": {"type": "number", "minimum": 0, "maximum": 1},
          "title": {"type": "string", "minLength": 1, "maxLength": 300},
          "body": {"type": "string", "minLength": 1, "maxLength": 4000}
        }
      }
    }
  }
}
